# Инварианты «Клинча»

Пороги, установленные замерами. Каждый — ответ на реальную поломку, которую
глаз ловил позже, чем число. Прогон всего: `sh tools/check.sh` (нужен node
с Playwright и Chromium; `cd tools && npm install` ставит нужную версию).
Один пробник: `node tools/live.js`. На каждый push в ветку GitHub Actions
гоняет тот же `check.sh`, и деплой на Pages ждёт зелёного прогона.

## Правила пробников

- **Мерить нарисованный скелет, не позный.** Под физикой (`b.rd`) боец
  рисуется из `ragJ`; пробник читает `b.drawJ || poseOf(b)`, никогда `b.J`.
  Эта ошибка дважды стоила полного цикла диагностики.
- **Живой цикл, не обходной.** `frame()` шагает пружины сам; `settle()` —
  только для сцен без времени. Всё, что должно жить в каждом кадре
  (базовая цель `Sp.t0` для предохранителя `limbSet`), лежит в `Sp.step`.
- **Случайность только в правилах, и с зерном.** Кубик правил — `rng()`
  с зерном боя (`fightSeed`), поэтому бой целиком восстанавливается из
  зерна и ходов (код боя). Анимация читает запись раунда (`rec`); повтор
  и пересмотр обязаны воспроизводить бой до цифры. Бот и выбор архетипа
  CPU остаются на `Math.random()`: их решения попадают в код боя явно.
- **Сначала число, потом код.** Порог «пройдено/нет» пишется до правки,
  меряется тем же пробником после.

## Пороги

| Пробник | Что | Порог |
|---|---|---|
| `reg` | бокс по фиксированному сценарию | совпадает с `tools/baseline/boxing.txt` до цифры (сценарий с фиксированным зерном 12345; эталон снят после правки апперкота) |
| `reg` | ММА | записи, состояние и позиция не меняются после повтора |
| `save` | код боя | тот же код в свежей странице даёт те же записи (бокс и ММА), доигрывание не расходится, автосохранение снимается по окончании, кнопки «продолжить» и «присланный бой» работают |
| `moves` | приёмы | 22 различимых движения (партер, клинч, стойка с хуком), конечности в пределах длины костей |
| `ext` | размах | суставы не дальше длины кости, ничего не уходит за сцену |
| `live` | удар ногой в голову | стопа не ниже `голова − HEAD_R − 2` в реальном `playRound` |
| `live` | передние стопы | никогда не пересекаются (зазор > 0; сейчас 8–11) |
| `live` | победная поза | обе кисти выше головы |
| `strike` | дуга удара | извилистость > 1 (не прямая), замах растёт с уроном (прямой 7 → хук 12 → апперкот 12 → кросс 33), сустав ≤ 30°/кадр; прямой и хук меряются по ведущей руке |
| `step` | шаг | стопа трогается раньше таза (183–283 мс против 400–517), скольжение опоры (доля кадров опоры со скоростью > 15 ед./с): джеб ≤ 0.15, кросс ≤ 0.12, остальные ≤ 0.14 |
| `react` | реакция на попадание | смещение головы растёт с уроном (прямой 6 → хук 9 → апперкот 9 → кросс 17 → вертушка 23), у лёгких ударов — позой, у тяжёлых — физикой; брызги слюны (частицы `s`) только на ударе в голову с уроном ≥ 24, не меньше шести |
| `trans` | переходы партера | конец конечности ≤ 5 ед./кадр |
| `rag` | тряпичная кукла | на стаггере и нокауте локоть и колено сгибаются (минимум < 165°) и не выворачиваются (12°…190°; предел шарнира 149°, бленд к позе даёт ещё 2–3°, у лежащего колено складывается до 15°); конец конечности ≤ 9 ед. за кадр (нормировано на кадр 60 к/с по реальному dt) ПОСЛЕ импульса — первые два кадра физики и есть удар, там конечность рвёт на 6–15 ед. от прогона к прогону, это печатается отдельно |
| `kofall` | нокаут | падение начинается ПОСЛЕ панели «НОКАУТ» и видно не меньше 20 кадров; под панелью тело держит реакцию позой; в конце лежит |
| `clinch` | вход в клинч и выход | нарисованная стопа трогается раньше таза; таз ≤ 2 ед./кадр; таз–стопа ≤ длины ноги; приходит на метку ± 0.5 |
| `stance` | живая стойка | переступ смещает ступню ≤ 3, **метка не двигается вовсе** (иначе меняется дистанция удара и повтор расходится), ступня отрывается от настила в каждом переступе (подъём > 0.4; при 60 кадрах ~1.5); не оценивается переступ, чьё окно обрезано концом трассы или в котором выпал кадр (интервал > 40 мс), но оценённых должно быть ≥ 3; зазор передних стоп ≥ 6 |
| `bot` | уровни бота | случайный игрок, 600 боёв на уровень: доля его побед строго убывает лёгкий → средний → трудный с зазором ≥ 5 пунктов (замер с хуком: 53 / 39 / 25; до хука 56 / 34 / 22) |
| `marks` | следы боя | считаются от записей раунда (`marksUpTo`), растут монотонно, рассечение ⇔ хотя бы одно тяжёлое (≥ 24) попадание в голову, свежее рассечение получает время (струйка растёт), в конце живого размена совпадают с формулой по записям — значит, в повторе те же |
| `sound` | синтез звука (офлайн-рендер) | удар: пик 0.35–0.98, длительность ≤ 0.9 с, доля энергии ниже 300 Гц ≥ 0.5, переходов через ноль ≤ 1400/с (замер 255–290); слабый удар тише сильного, в корпус ниже; хруст: ≥ 3 атаки в первых 120 мс (замер 4–5), ZCR вдвое выше удара (замер 2100–2350 против 270), ≤ 0.5 с. Щелчки считаются по АТАКАМ огибающей, а не по пикам выше доли максимума: щелчок шумовой, его пик случаен, и абсолютный порог давал то два, то четыре — пробник мерил реализацию шума. Сама очередь тоже детерминирована (шаг 24 мс), хвостовое шипение идёт после неё |
| `depth` | глубина (ось z) | кости держат длину в пространстве (ошибка ≤ 0.05), кулак кросса приходит на линию удара (|z| ≤ 1.5) настоящим поворотом: в контакте бьющее плечо на линии удара (|z| ≤ 1), переднее уходит к зрителю (z ≥ 4; замер 5.8 / 0), кулак прямого летит по центру (z 0, раньше 8.2), стопа ноги в замахе перед корпусом (z ≥ 4; замер 6.2), стопа вертушки за корпусом (z ≤ −4; замер −7.8), хук: кулак в размахе выходит наружу (z ≥ 4; замер 9.5) и приходит на линию удара (|z| ≤ 2.5; замер 0.7), ведущая рука в размахе выше корпуса; проекция разводит плечи по экрану (dy > 0.5) |
| `lay` | вёрстка | 7 экранов × 13 состояний без переполнения и наложений |
| `soak` | четыре полных боя через интерфейс | каждый бой доходит до финала (иначе застрявший экран, как глохшая кнопка «В ГОЛОВУ», считался успехом), без ошибок страницы |
| `filmperf` | киношный слой | ярусы сами понижаются, кадр ≥ 30 fps на iPhone SE |
| `load` | загрузка | стартовый экран на Fast 3G быстрее 15 с (замер: 2.7 с при 31.4 до; хвост с картинками и музыкой докачивается ещё ~19 с) |

## Известное, не починенное

- Переходы партера: сустав до 49°/кадр при конце конечности 4.6 — глаз
  видит второе; оставлено.
- Глубина лёгкая (`EL .12`, `PSP .008`): камера почти фронтальная, повороты
  читаются по слоям и по толщине конечностей, а не по перспективе.
